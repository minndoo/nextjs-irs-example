import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <h1>
      Welcome. This app/web is an example usage of incremental static
      regeneration by Next.JS
    </h1>
  );
};

export default Home;
